# Beta2 验收记录

最新更正：上一个正式版为 1.2.4，用户最终指定本包为 **1.2.5 Beta2**。应用版本名与交付文件均已改为 `1.2.5_beta2`，versionCode 仍为 31。完整 Release 构建通过（4 分 28 秒），签名校验及 APK 内部版本核对通过。最终文件：根目录 `SleepDown课程表_v1.2.5_beta2.apk`。以下更正过程中的旧名称均不再作为交付版本。

最终命名更正：用户指定 **1.2.4 Beta2**。已将应用版本名改为 `1.2.4_beta2`，保留 versionCode 31、原包名和签名；重新执行完整 `assembleGithubRelease` 成功（5 分 42 秒），APK 签名及内部版本检查通过。最终交付为根目录 `SleepDown课程表_v1.2.4_beta2.apk`。以下 `1.4.3` 交付名及其哈希属于更名前历史记录，已被新包取代；本轮只修正版本名，未重复执行已通过的 116 项业务测试。

## 已有 AI 修复验收

复核托管配置、MiMo 参数策略、Chat/Responses 空数组字段及 DeepSeek 工具选择：地址、协议和模型继续服从管理平台；不改写托管完整地址。原有 18 项 AI 定向测试通过。用户报告昨晚修复已实机通过；本次没有使用真实凭据重新调用模型，也没有重新采集设备性能。

## 图标

保留根目录四张用户原图，使用 `scripts/crop_launcher_icons.py` 可重复生成五档密度资源。取消色差猜测裁切：简约深浅共用 1254 基准的 `(85,85,1169,1169)`，看板娘深浅共用 `(42,38,1212,1208)`，保持正方形及透明连续圆角。已人工检查 192px 和 48px 预览；简约按用户反馈增加留白。设置及两种关于页使用当前风格/深浅选择。修复原预览函数访问对象私有常量导致的编译错误。原 Launcher 别名保留，沿用先禁用旧别名、再启用唯一新别名的链路。

## 更新渠道

通用设置 → 系统行为 → 接收 Beta 版更新，默认关闭。关闭只选择正式版，开启包含正式版与预发布；手动和每日自动检查共用规则。切换渠道清除检查日期及红点缓存，下一次检查使用新渠道。

Gitee 发布列表分页读取、按版本排序，不依赖列表顺序。2026-09-09 只读实查接口返回 20 个正式版本，最新为 v1.2.4；未发布新版本。版本主号和 beta/rc 序号分离，`1.4.3 > 1.4.3_beta10 > 1.4.3_beta2`；更高主版本优先，构建元数据不触发更新。六项版本/渠道测试通过。

本包 applicationId 和 Release 签名不变，versionName=`1.4.3_beta2`、versionCode 从 30 增至 31。正式版发布必须使用不小于已分发 Beta 的 versionCode，建议每次分发都递增。检查更新依据版本名称与渠道，安装仍受 Android 版本编号限制：[Android 官方版本规则](https://developer.android.com/studio/publish/versioning)。Beta 不创建独立包名或渠道 flavor；商店版继续禁用 APK 自更新。

## 深色居中弹窗

仅修改公共居中 Alert 的按钮底色：深色主按钮 alpha 0.82，次要/危险按钮底色 alpha 0.76；文字、浅色样式和其他按钮不变。设备视觉验收未执行。

## 今日助手追加修复

- 学期/课程工具结果缺失备注，模型无法判断哪些记录有备注；增加备注字段，沿用现有不可信文本边界。
- 原备注更新将空串回退为旧值；现在显式空串/空白清空，省略/null 保留。提示词要求全学期批量任务为每个真实 ID 准备 ALL_WEEKS 操作，保留其他课程属性，仍经用户确认及原有事务/验证链路。
- Responses 空 output/content 使用已有可空数组解析。完成事件可补齐最终正文，避免只有 completed 正文时误判为空；与 delta 重复的正文不重复显示。incomplete 明确停止，不接受截断计划；失败事件读取嵌套服务端错误。异常时关闭流连接。
- 新用例覆盖跨周同名课程全部清空、其他字段保持、未修改数据无法通过执行验证、省略/null/空白语义、工具备注可见、可空输出、completed-only、重复文本和 incomplete。

源码中未找到“未知回复”固定文案，已向用户询问完整提示。上述是确认存在的兼容缺陷，不能声称已复现用户这句描述对应的具体错误。

## 构建状态

图标阶段 58 项测试、更新阶段 64 项测试和各自完整 Release 构建通过。追加助手修复后最终 116 项测试全部通过（0 失败、0 错误、0 跳过），`compileGithubReleaseKotlin` 和 `assembleGithubRelease` 成功，构建耗时 5 分 40 秒。扩展测试首次发现旧自定义时间断言仍要求长文本格式，已更新为验证既有紧凑字段说明及完整时间值。使用 `tmp/ai-managed-tests.init.gradle` 筛选相关测试，不是全仓测试。保持 R8、资源压缩、lintVital 与 Release 签名。

本次不修改数据库、后端及学校适配链路；最终检查 APK 中不存在本地 LYIT 文件、protobuf/YAML/JSON 索引记录。APK v2 签名校验成功，aapt2 验证包名 `com.xiaomanjun.sleepdownschedule`、版本名 `1.4.3_beta2`、版本编号 31。未安装、未推送、未发布，新增助手/弹窗/渠道的实机验收未执行。

交付：根目录 `SleepDown课程表_v1.4.3_beta2.apk`，6,670,305 字节。

SHA256：`bfae204fda9b0a800844801441d80bbec25f5271fef68711ad57603922fea5f3`。
