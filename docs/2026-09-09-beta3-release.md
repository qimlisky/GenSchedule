# 1.2.5 Beta3 发布核验

用户授权：整理当前工作树、合入主分支并发布 Beta；明确要求 versionCode 与 Beta2 相同。

- 版本：`1.2.5_beta3`，versionCode **31**。包名及 Release 签名沿用 Beta2。
- 实时活动创建时读取当前图标风格与深浅选择；切换图标后，仅在资源不同且已有实时活动时，重建原通知并更新图标，不改变进度、操作和 ID，不重启服务。
- application icon/roundIcon 固定为看板娘风格；仍有系统 night 资源变体。桌面六个已有别名保持兼容。
- Beta2 的更新判断按主版本、预发布阶段、序号排序，`1.2.5_beta3 > 1.2.5_beta2`，不读取 versionCode。
- 本次不安装 Beta3，保留手机 Beta2 供用户从应用内测试更新。

当前源码整理包含此前已在本地包中的玻璃库源码补丁、界面文件拆分、AI 兼容与助手修复、实时活动进度、图标与更新渠道，以及工作树已有学校资源精简。删除的学校本地适配文件不恢复、不重新打入包；洛阳理工留存位于本地 patches 子目录，不进入 APK。

提交排除：私有后端、签名、缓存、本地 Miuix checkout、临时目录、设备截图、APK 与其他个人文件。通过本地 `.git/info/exclude` 保留这些文件，未删除。图标原稿、生成脚本、Kyant 许可证和上游记录随源码保留。

验证：137 项定向单元测试全部通过。全仓测试曾因旧测试的包迁移导入问题无法编译，不能宣称全仓通过；本轮范围内的首页性能测试已修正导入并纳入 137 项。Release 完整构建、APK 签名及发布 URL 在发布时另行核验。未进行设备视觉/性能复测；用户负责 Beta2 → Beta3 的实机更新验收。

完整 Release 构建成功（5 分 34 秒），签名验证通过，APK 内部版本名/编号核对为 `1.2.5_beta3`/31，包内无本地 LYIT 文件或索引记录。APK 为 6,539,937 字节，SHA256：`ae734fad2380a88b80d0c6785c1c4c7093c9bcb97255fe6b92e2a1dc17335ae6`。

发布渠道：GitHub 与应用内实际读取的 Gitee，均标记预发布，不替换正式版 latest。
